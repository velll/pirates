class BoardBuilder {
  ctx: CanvasRenderingContext2D;

  constructor(ctx: CanvasRenderingContext2D,
              initialWidth: number,
              initialHeight: number) {
    
    this.ctx = ctx;
  }

  build(): {} {
    return {}
  }
}

export { BoardBuilder }